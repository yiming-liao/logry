import type { HandlerLoggerConstructorOptions } from "@/core/logger/handler-logger";

/** Options for constructor in Logger  */
export type ParentLoggerConstructorOptions = HandlerLoggerConstructorOptions;
